#!/usr/bin/env ruby

ruby_files = %w[
  **/*.rb
  **/*.ru
  bin/bridgetown
  bin/format-ruby
  bin/setup
  bin/yarn
  Rakefile
].map { |p| Dir.glob(p) }.flatten.compact.sort

command = %w[bundle exec rufo] + ARGV + ruby_files
system *command
exit($?.exitstatus)
